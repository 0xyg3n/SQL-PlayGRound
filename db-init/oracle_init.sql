ALTER SESSION SET CONTAINER = FREEPDB1;
CREATE TABLE users(id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, username VARCHAR2(50), password VARCHAR2(50));
INSERT INTO users(username,password) VALUES('admin','password');
COMMIT;
